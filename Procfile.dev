# To run:
# foreman start -f Procfile.dev

web: rails server -p $APP_PORT
webpack: cd client && nodemon webpack.hot.js

ngrok_webpack: ngrok http -subdomain=$ASSETS_SUBDOMAIN $ASSETS_PORT
ngrok_rails: ngrok http -subdomain=$APP_SUBDOMAIN $APP_PORT
